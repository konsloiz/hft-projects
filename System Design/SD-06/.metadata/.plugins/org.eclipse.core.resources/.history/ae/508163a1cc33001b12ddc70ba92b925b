package streamsCreation.version1;

import java.util.stream.IntStream;

public class Main {

	public static void main(String[] args) {
		
		IntStream sqr = IntStream.range(1, 10);
		
		sqr.map(s ->s*s).boxed().peek(l -> System.out.println(l)).reduce(0, (s,l) -> s + l);
		
	}

}
